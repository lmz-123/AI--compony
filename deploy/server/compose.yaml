services:
  claudeteam:
    build:
      context: ../..
      dockerfile: deploy/server/Dockerfile
    image: ai-company:local
    init: true
    restart: unless-stopped
    volumes:
      - ../../src:/app/src:ro
      - ../../team-data:/data
      - /srv/ai-company-artifacts:/data/artifacts
      - ../../projects:/workspace/projects
      - ../../server-secrets/codex/config.toml:/root/.codex/config.toml:ro
      - ../../server-secrets/ssh:/root/.ssh:ro
      - ./entrypoint.sh:/opt/ai-company/entrypoint.sh:ro
    environment:
      CLAUDETEAM_LARK_SEND_AS: bot
      CLAUDETEAM_MONITOR_HOST: 0.0.0.0
      CLAUDETEAM_MONITOR_PORT: "8765"
      CLAUDETEAM_ADMIN_HOST: 0.0.0.0
      CLAUDETEAM_ADMIN_PORT: "8766"
      LARK_CLI_NO_PROXY: "1"
    network_mode: host
    tty: true
    stdin_open: true
    command: ["/bin/bash", "/opt/ai-company/entrypoint.sh"]
    healthcheck:
      test: ["CMD", "claudeteam", "health"]
      interval: 60s
      timeout: 20s
      retries: 3
      start_period: 120s
